name: giphy_picker
description: A new Flutter package project.
version: 0.0.1
author:
homepage:

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  giphy_client: ^0.0.1
  http: ^0.11.0+1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  assets:
    - assets/