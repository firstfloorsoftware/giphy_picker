name: giphy_picker
description: A plugin that allows you to pick animated GIF images from Giphy.
version: 0.0.1
author: First Floor Software <info@firstfloorsoftware.com>
homepage: https://github.com/firstfloorsoftware/giphy_picker

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  giphy_client: ^0.0.1
  http: ^0.11.0+1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  assets:
    - assets/